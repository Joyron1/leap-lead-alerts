# Copy to .env.local (git-ignored). Both values are public by design: the publishable key only
# grants what the RLS policies allow, which is nothing until a signed-in, allow-listed user.
VITE_SUPABASE_URL=https://tipuzwoirrzlibplmbji.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
